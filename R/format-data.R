make_comparecube <- function(cube.new,
                             cube.old,
                             outliers = TRUE,
                             dumps = NULL){

  if(is.null(dumps)) dumps <- getOption("qualcontrol.dumps")

}
